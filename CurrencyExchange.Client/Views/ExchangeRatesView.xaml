<UserControl x:Class="CurrencyExchange.Client.Views.ExchangeRatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B0E11">

    <UserControl.Resources>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#EAECEF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#0F1317"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#1E2329"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="RowHeight" Value="44"/>
            <Setter Property="ColumnHeaderHeight" Value="40"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#161A1E"/>
            <Setter Property="Foreground" Value="#848E9C"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16 0"/>
            <Setter Property="BorderThickness" Value="0 0 0 1"/>
            <Setter Property="BorderBrush" Value="#1E2329"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="16 0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#EAECEF"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1E2329"/>
                    <Setter Property="Foreground" Value="#F0B90B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A1D21"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="RefreshBtn" TargetType="Button">
            <Setter Property="Background" Value="#F0B90B"/>
            <Setter Property="Foreground" Value="#0B0E11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16 8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D9A800"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="28 24 28 28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0 0 0 20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Market Overview" FontSize="20" FontWeight="Bold"
                           Foreground="#EAECEF" FontFamily="Segoe UI"/>
                <TextBlock Text="Live rates from NBP · Updated daily" FontSize="12"
                           Foreground="#848E9C" FontFamily="Segoe UI" Margin="0 4 0 0"/>
            </StackPanel>

            <Button Grid.Column="1" Content="↻  Refresh"
                    Command="{Binding LoadRatesCommand}"
                    Style="{StaticResource RefreshBtn}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Error -->
        <Border Grid.Row="1"
                Background="#2A1A1A" CornerRadius="4"
                Padding="14 10" Margin="0 0 0 12"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#F6465D"
                       FontFamily="Segoe UI" FontSize="13"/>
        </Border>

        <!-- Loading bar -->
        <ProgressBar Grid.Row="2"
                     IsIndeterminate="True" Height="2"
                     Foreground="#F0B90B" Background="Transparent"
                     BorderThickness="0" Margin="0 0 0 8"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Table -->
        <Border Grid.Row="3" CornerRadius="8" Background="#161A1E"
                BorderBrush="#1E2329" BorderThickness="1">
            <DataGrid ItemsSource="{Binding ExchangeRates}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="CURRENCY" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="30" Height="30" CornerRadius="15"
                                            Background="#1E2329" Margin="0 0 10 0">
                                        <TextBlock Text="{Binding CurrencyCode}"
                                                   FontSize="9" FontWeight="Bold"
                                                   Foreground="#F0B90B"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding CurrencyCode}"
                                                   FontWeight="SemiBold"
                                                   Foreground="#EAECEF" FontSize="13"/>
                                        <TextBlock Text="{Binding CurrencyName}"
                                                   Foreground="#848E9C" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="PRICE (PLN)" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Mid, StringFormat=N4}"
                                           FontWeight="SemiBold"
                                           Foreground="#0ECB81"
                                           VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>